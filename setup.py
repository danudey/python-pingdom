#!/usr/bin/env python

from setuptools import setup, find_packages
setup(name='pingdom',
		version = 0.1,
		description="Python interface to the Pingdom API",
		include_package_data=True,
		zip_safe=False,
      )